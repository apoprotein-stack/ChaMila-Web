- 名称： Build with Vite（与 Vite 共建）
        # 直接在根目錄執行即可，因為我們在 config 裡寫好路徑了
 运行： NPM 安装 & NPM 运行构建

 - 名称： 上传文物
        uses: actions/upload-pages-artifact@v3
        with:
          # 指向根目錄的 dist
          path: ./dist
